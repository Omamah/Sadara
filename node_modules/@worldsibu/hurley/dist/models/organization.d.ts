import { Channel } from './channel';
import { User } from './user';
import { Peer } from './peer';
export declare class OrganizationOptions {
    channels: Channel[];
    peers: Peer[];
    users: User[];
}
export declare class Organization {
    name: string;
    channels: Channel[];
    peers: Peer[];
    users: User[];
    constructor(name: string, options: OrganizationOptions);
}
