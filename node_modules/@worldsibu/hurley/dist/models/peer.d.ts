export declare class PeerOptions {
    number: number;
    ports: string[];
    couchDbPort: string;
}
export declare class Peer {
    name: string;
    options: PeerOptions;
    constructor(name: string, options: PeerOptions);
}
