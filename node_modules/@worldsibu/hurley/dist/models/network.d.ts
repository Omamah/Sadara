import { Organization } from './organization';
import { Channel } from './channel';
import { User } from './user';
export declare class NetworkOptions {
    hyperledgerVersion?: string;
    externalHyperledgerVersion?: string;
    channels?: number;
    organizations?: number;
    users?: number;
    inside?: boolean;
    networkConfigPath?: string;
}
export declare class Network {
    path: string;
    options: NetworkOptions;
    organizations: Organization[];
    channels: Channel[];
    constructor(path: string, options: NetworkOptions);
    init(): Promise<void>;
    buildNetwork(organizations: number, channels: number, users: number): void;
    buildFromSave(organizations: Organization[], channels: Channel[], users: User[]): void;
    buildNetworkFromFile(networkConfigPath: string): Promise<void>;
    initChannels(config: any): void;
    initOrgs(config: any): void;
}
